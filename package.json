{
    "type": "module",
    "scripts": {
        "dev": "tsup src/server.ts --format=esm --watch --onSuccess \"node dist/server.js\"",
        "build": "tsup src/server.ts --minify --format=esm",
        "start": "node dist/server.js"
    },
    "devDependencies": {
        "tsup": "^8.0.1",
        "typescript": "^5.3.2"
    }
}
